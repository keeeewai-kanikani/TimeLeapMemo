# TimeLeap Memo 外部仕様書

## 1. 概要
TimeLeap Memoは、「昨日の自分」が今に割り込む、時間軸に基づいたメモ・スケッチアプリケーションです。
単なるメモ帳ではなく、思考を「線」ではなく「層」として扱い、時間の経過とともに記憶が薄れていく「恒常性（Homeostasis）の忘却」と、過去の思考プロセスを再体験できる「タイムリープ（時間移動）」を体験の核としています。

## 2. 主要機能

### 2.1 描画システム
- **フリーハンド描画**: マウス（またはペンタブレット）による手書き描画。
- **平滑化処理**: 中点連結による2次ベジェ曲線変換を行い、滑らかなストロークを実現。
- **恒常性の忘却（Homeostatic Forgetting）**:
    - ストロークには作成時の「仮想時刻（Virtual Time）」が記録されます。
    - 現在の仮想時刻から時間が経過するほど、ストロークは指数関数的に透明度が増し、最終的に消失します（指数減衰: $e^{-\lambda \cdot age}$）。
- **描画制限**: 動作の軽量化のため、表示される合計頂点数に制限を設け、常にサクサクとした書き心地を維持します（古いストロークから非表示化）。

### 2.2 タイムラインと時間移動（Time Leap）
- **仮想時刻（Virtual Time）**: アプリケーション内の経過時間。メモを取るたびに前進します。
- **再生/一時停止**: 過去に取ったメモを、描いた順序（時間軸）に沿ってプレイバックします。
- **ナウ（Now）機能**: 最後に書き込んだ最新の時刻へ瞬時にジャンプします。

### 2.3 マルチバースパッド（Chaos Pad）
- **空間・時間統合ナビゲーション**:
    - `B` キーを押下中、キャンバス全体が「マルチバースパッド」として機能します。
    - **X軸**: 時間軸の操作。左右に動かすことで過去・未来へ瞬時に移動。
    - **Y軸**: 「世界線（並行世界）」の選択。異なるタイミングで描かれたストロークの集まり（セグメント）をハイライト表示し、思考の枝分かれを視覚化します。

### 2.4 インポート・エクスポート
- **JSON形式**: ストロークデータ（座標、筆圧、色、作成時刻など）をJSONファイルとして保存・読み込みが可能。
- **自動保存**: 最後に使用したフォルダパスを環境設定として保持。

## 3. 操作仕様（ショートカットキー）

| キー | 機能 | 説明 |
| :--- | :--- | :--- |
| `C` | クリア | キャンバス内のすべてのストロークを消去し、時間をリセットします。 |
| `N` | ナウ | 最新の記録時刻まで時間をジャンプさせます。 |
| `B` | マルチバース | ホールド中、X軸で時間、Y軸で世界線の選択操作を行えます。 |
| `Space` | (代替) | Bキーと同様にカオスパッドモードを起動できるように検討・実装。 |

## 4. ユーザーインターフェース

- **メインキャンバス**: ModernGLを使用した高速なOpenGL描画エリア。
- **タイムラインウィジェット**: 
    - 下部のスライダー付近に重なって表示される透過ウィジェット。
    - ストロークの密度（思考の密度）をグラフ化して表示。
    - 複数の「世界線（セグメント）」を段として表示。
- **操作パネル**:
    - タイムスライダー: 現在時刻のシーク。
    - 再生ボタン: 記録の自動再生。
    - エクスポート/インポートボタン: ファイル操作。

## 5. 技術仕様

- **開発言語**: Python 3.8+
- **GUIライブラリ**: PyQt6
- **描画エンジン**: ModernGL (OpenGL 3.3+)
- **データ管理**: NumPy (頂点計算), JSON (保存形式)
- **ビルドシステム**: Nuitka (スタンドアロンexe化)
- **設定ファイル**: `%LOCALAPPDATA%\TimeLeapMemo\config.json`

## 7. 次世代ファイル管理：シームレスな時間軸統合

### 7.1 分割なき「全時間軸」管理
従来の「ファイル保存・読み込み」という概念を廃止し、すべてのメモを一連の巨大な時間軸の中に配置します。
データーは時間単位、あるいは日単位で自動的に内部保存されますが、ユーザーには「ファイル」という単位を意識させず、過去から現在までを一つの連続した流れとして提示します。

### 7.2 フラクタル・タイムライン（階層型UI）
マルチバースパッドの操作を通じ、異なる時間スケールをなめらかに拡大・縮小（ズーム）して移動できます。

- **階層構造**:
    - **最上段**: 直近1時間の詳細なタイムライン。
    - **中段以降**: 逆八の字型にラインが伸び、下に向かって「1日」「1週間」「1ヶ月」とより大きなスケールのタイムラインが入れ子状に並びます。
- **ナビゲーション**:
    - 縦スクロール（またはズーム操作）によって、ミクロな思考の記録からマクロな月単位の履歴までをなめらかに往来可能です。

### 7.3 世界線ナビゲーションの最適化
世界線の数が増えた際の視認性と操作性を向上させるため、以下の動的レイアウトを導入します。

- **適応型レイアウト (Adaptive Layout)**:
    - タイムラインをスクロールした際、画面内に表示されている「最も古い世界線」が自動的に一番上に配置されるよう、表示位置を動的に再計算します。これにより、膨大な履歴があっても常に現在の文脈に関連する世界線が手元に集まります。
- **イージング・アニメーション (Easing Animation)**:
    - タイムラインの移動や世界線の再配置が行われる際、イージング（加減速）の効いたなめらかなアニメーションを適用します。これにより、急激な視覚的変化を抑え、ユーザーが時間軸の「繋がり」を直感的に把握できるようにします。

### 7.4 インプレッションタグ（Impression Tags）
「ファイル名の指定」に代わる検索・管理手段として導入します。

- **メタデータ**: 特定の時間軸や世界線（ストロークの集まり）に対して、ユーザーが任意のタイトルやキーワードを付与できます。
- **検索機能**: インプレッションタグによる検索・ジャンプ機能を備え、膨大な履歴の中から目的の思考プロセスを瞬時に呼び出すことができます。
- **自動保存**: 一定の時間間隔、またはインプレッション付与時にバックグラウンドで自動的に状態をシリアライズし、保存し忘れを防止します。

## 6. Androidタブレット移植に向けた検討事項

### 6.1 技術スタックの選択案
移植にあたって、以下のいずれかのアプローチが考えられます。

1.  **Python継続（PySide6/PyQt6 + ModernGL）**:
    - `pyside6-android-deploy` を使用し、既存のロジックを最大限流用する。
    - ModernGLはAndroidのOpenGL ES環境でも動作可能（要検証）。
2.  **Kivy / BeeWare への移行**:
    - モバイルUIに特化したPythonフレームワークへの書き換え。
3.  **ネイティブ移植（Kotlin + Jetpack Compose + OpenGL ES）**:
    - パフォーマンスとペン入力（S Pen等）の最適化を最優先する場合。
    - 描画ロジックのシェーダーコードは移植可能。

### 6.2 UI/UXの変更点
- **入力インターフェース**:
    - **左手用フローティングボタン**: 物理キーボードがないため、左手で常に押しやすい位置（左右反転設定も考慮）に「消しゴム」「マルチバース」「ナウ」のフローティングボタンを配置します。
- **マルチバースパッドの挙動**:
    - **トグル式展開**: PC版のようなキーホールド（長押し）ではなく、一度ボタン等で展開したら、画面を短くタップするまで操作モードを維持する「スティッキー・モード」を導入します。
- **消しゴムツールへの変更**:
    - **ストローク消去**: PC版の「クリア（全消去）」ボタンを廃止し、触れた線（ストローク）を個別に消去する「消しゴム」ツールに変更します。
    - **履歴の保持**: 消しゴムで消された線もタイムライン（履歴）データには残り続け、過去に遡ることで再び表示・確認が可能です。
- **画面回転の仕様**:
    - **UI（マルチバースパッド）のみ回転**: タブレットの向きに合わせて操作系UIのみが回転し、メモ（描画内容）自体は固定または自由な角度から閲覧できるようにします。

### 6.3 ハードウェア・互換性
- **OSバージョン**: Android 8.1 以上をターゲットとします。
- **ポインティングデバイス**:
    - スタイラスペン（筆圧検知）は必須とせず、一般的なタッチ用スライドペンや指での描画に最適化します。
    - 筆圧検知には対応せず、一定の太さ、または速度に応じた擬似的な太さの変化で対応します。

### 6.4 最適化方針
- **自動最適化**: GPUアーキテクチャへの最適化（タイル・ベース・レンダリング対応）や省電力制御（描画負荷の動的調整）など、技術的に複雑な最適化については開発側（AI）にて自動的に実施・管理します。
